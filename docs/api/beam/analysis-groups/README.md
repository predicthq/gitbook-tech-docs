# Analysis Groups

Analysis Groups aggregate Feature Importance across many per-location analyses to produce one consistent feature set - for fleets of locations sharing a single model, where every location must feed the model identical inputs.

Use a group only when a shared model requires it; individual per-location analyses give better-calibrated results in most cases. Start with [Get Aggregated Feature Importance](get-aggregated-feature-importance.md).
